import { TOKENS } from "@/garden/tokens";

/** Largura padrão da coluna do trilho (nó + linha centrados). */
export const TIMELINE_RAIL_TRACK_DEFAULT_WIDTH_PX = TOKENS.size[16];
/** Espessura padrão da linha vertical. */
export const TIMELINE_RAIL_TRACK_DEFAULT_STROKE_PX = TOKENS.spacing[2];
/** Diâmetro padrão do nó quando se usa `lineExtent="toDot"` no cálculo da altura da linha. */
export const TIMELINE_RAIL_TRACK_DEFAULT_DOT_PX = TOKENS.size[12];
