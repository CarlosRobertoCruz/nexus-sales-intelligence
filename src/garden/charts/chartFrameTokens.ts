/**
 * Cores de grade / texto usadas em SVGs de gráfico (garden).
 * Cada app mapeia o seu tema (ou tokens) para isso.
 */
export interface ChartSvgFrameTokens {
  gridLine: string;
  textMuted: string;
  textPrimary: string;
  textSubtle: string;
  tooltipBackground: string;
  tooltipBorder: string;
  tooltipInverseBackground: string;
  tooltipInverseText: string;
}
